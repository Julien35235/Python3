# Définir la variable de départ
letter = ord('a')

# Définir la limite de boucle
while letter <= ord('z'):
    # Afficher la lettre actuelle
    print(chr(letter) + " &((§è(%??+/.?*£__@#<>£`(è( ", end='')
    # Passer à la lettre suivante
    letter += 1
